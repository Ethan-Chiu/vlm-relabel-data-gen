# Copy this file to .env and fill in your values
# .env is gitignored — never commit secrets

# --- API Keys (set whichever backend you use) ---
DATAGEN_OPENAI_API_KEY=sk-...
DATAGEN_GEMINI_API_KEY=...

# --- Optional overrides (also configurable in config.toml) ---
# DATAGEN_VLM_BACKEND=openai
# DATAGEN_VLM_MODEL=gpt-4o-mini
# DATAGEN_CONCURRENCY=8
